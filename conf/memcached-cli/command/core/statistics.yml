inherits:
  - command.core.memcached
options:
  format:
    name: format
    mode: 4
    default: table
    possible-values: ['table', 'json']
    description: 'Display result as table or json'
validators:
  statistics-formatter: in2pire.memcached.cli.validator.statistics-formatter
